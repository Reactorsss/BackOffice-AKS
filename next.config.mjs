/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['admin.angkasangga.ph'],
    },
    basePath: '',
    assetPrefix: '',
  };
  
  export default nextConfig;
  